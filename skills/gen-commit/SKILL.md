---
name: gen-commit
description: 分析代码改动并生成结构化 commit message
---

# Generate Commit Message

分析代码改动，生成符合项目规范的结构化 commit message。

## 用法

```
/gen-commit [target]
```

**参数：**
- (无参数) - 分析当前未提交的改动 (`git diff`)
- `last` 或 `HEAD` - 分析最近一次提交
- `<commit-hash>` - 分析指定提交（如 `abc123`）

## 指令

你是一个 commit message 生成器。分析代码改动并按以下步骤生成 commit message：

### 第一步：收集改动信息

根据参数执行对应的 git 命令：

```bash
# 未提交的改动（默认，无参数）
git diff --stat
git diff

# 最近一次提交（参数: "last" 或 "HEAD"）
git show HEAD --stat
git show HEAD --format="" -p

# 指定提交（参数: commit hash 如 "abc123"）
git show <commit-hash> --stat
git show <commit-hash> --format="" -p
```

### 第二步：分析改动

分析 diff 内容，识别以下信息：

1. **改动类型**（选择一个）：
   - `feat` - 新功能
   - `fix` - Bug 修复
   - `pref` 或 `perf` - 性能优化
   - `refactor` - 代码重构
   - `style` - 代码风格
   - `docs` - 文档
   - `test` - 测试
   - `chore` - 构建/依赖等

2. **范围**（可选）：
   - 受影响的模块/组件名称（如 `Chat`、`健康检测`、`智慧屏`）
   - 若涉及多个区域可省略

3. **改动点**：
   - 具体修改了什么代码/逻辑
   - 影响的关键函数或组件

4. **影响点**（必须详细）：
   - 列出所有可能受影响的功能模块和具体页面
   - 格式：`功能模块 > 具体页面/组件`
   - 例如：
     - `健康检测 > 舌诊拍照页面`
     - `商城首页 > 商品列表组件`
     - `个人中心 > 会员信息页面`
   - 潜在副作用
   - 用户可见的变化

### 第三步：生成 Commit Message

遵循项目的 commit message 格式：

```
<type>(<scope>): <简要描述>

- <详细改动点1>
- <详细改动点2>

影响点：
- <功能模块> > <页面/组件>（影响说明）
- <功能模块> > <页面/组件>（影响说明）
```

**格式规则：**
- 标题行：尽量不超过 72 个字符
- 描述使用中文（匹配项目风格）
- 如有需求 ID 需包含（如 `35002 【前端 | APP】`）
- 多个要点用 `-` 分行
- **必须包含影响点部分**，列出所有可能受影响的功能模块和页面

### 第四步：输出格式

按以下结构输出分析结果：

```markdown
## 📊 改动分析

### 改动文件
| 文件 | 改动类型 | 说明 |
|------|----------|------|

### 改动点
- 改动点1
- 改动点2

### 影响点（详细列出可能受影响的功能页面）
| 功能模块 | 页面/组件 | 影响说明 |
|----------|-----------|----------|
| 模块A | 页面a | 影响描述 |
| 模块B | 组件b | 影响描述 |

---

## 📝 生成的 Commit Message

```
<type>(<scope>): <description>

- <detail 1>
- <detail 2>

影响点：
- <功能模块> > <页面/组件>（影响说明）
```

---

## 💡 建议的完整提交命令

\```bash
git add <files>
git commit -m "$(cat <<'EOF'
<generated commit message>
EOF
)"
\```
```

### 示例

**示例 1 - Bug 修复：**
```
fix(健康检测): 修复 syndromes 空值问题及舌诊术语调整

- ProfilePicture 组件使用可选链访问 syndromes，避免空值报错
- 舌诊术语调整："舌面照片"→"舌正面照片"，"舌下照片"→"舌反面照片"

影响点：
- 健康检测 > 舌诊拍照页面
- 健康检测 > 面诊结果展示组件
```

**示例 2 - 新功能：**
```
feat(智慧屏): 优化注册流程

- 抽取 BottomView 组件复用于注册成功弹框底部按钮视图
- 开卡弹框 cardTypeName 增加判空处理
- 新增 goHome/navigateHome 方法统一处理跳转逻辑

影响点：
- 智慧屏 > 注册流程页面
- 智慧屏 > 开卡弹框组件
```

**示例 3 - 性能优化：**
```
pref: 多余判断去除

- 系统判断多余了，因为 isYofoDevice()的判断已经包含了系统判断

影响点：
- 全局 > 设备类型判断逻辑
```

## 注意事项

1. 生成前必须阅读实际的 diff 内容
2. 考虑上下文和业务逻辑，不仅仅是语法变化
3. 如果改动过于复杂，建议拆分为多个提交
4. 匹配项目现有的 commit 风格
5. 描述使用中文以匹配项目惯例
6. **影响点是必填项**，必须详细列出所有可能受影响的功能模块和页面
